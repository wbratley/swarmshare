[project]
name = "meshplay-publisher"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "meshplay-core",
    "libtorrent>=2.0.13",
    "typer>=0.12",
]

[project.scripts]
meshplay-publisher = "meshplay_publisher.cli:app"

[tool.uv.sources]
meshplay-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meshplay_publisher"]
